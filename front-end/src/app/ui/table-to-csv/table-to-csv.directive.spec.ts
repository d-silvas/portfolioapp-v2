import { TableToCsvDirective } from './table-to-csv.directive';

describe('TableToCsvDirective', () => {
  it('should create an instance', () => {
    const directive = new TableToCsvDirective();
    expect(directive).toBeTruthy();
  });
});
